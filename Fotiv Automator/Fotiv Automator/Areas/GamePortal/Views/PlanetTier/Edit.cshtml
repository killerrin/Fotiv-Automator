@model Fotiv_Automator.Areas.GamePortal.ViewModels.Forms.PlanetaryTierForm
@{
    ViewBag.Title = string.Format("Edit Planetary Tier");
}

<div class="row">
    @using (Html.BeginForm())
    {

        <div class="hidden" id="anti-forgery-container">
            @Html.AntiForgeryToken()
        </div>

        <div class="col-md-6">
            @Html.ValidationSummary()

            <div>
                @Html.LabelFor(x => x.ID)
                @Html.DisplayTextFor(x => x.ID)
            </div>

            <div>
                @Html.LabelFor(x => x.Name)
                @Html.EditorFor(x => x.Name)
            </div>

            <div>
                @Html.LabelFor(x => x.BuildRate)
                @Html.EditorFor(x => x.BuildRate)
            </div>

            <input class="btn btn-default editPlanetaryTierButton" type="submit" value="Edit Planetary Tier" />

            <a href="@Url.Action("Delete", "PlanetTier", new { planetTierID = Model.ID})"
               class="btn btn-danger"
               data-delete="THIS ACTION IS FINAL. Are you sure you want to delete this Planetary Tier?">
                <i class="glyphicon glyphicon-remove"></i> Delete
            </a>
        </div>
    }
</div>