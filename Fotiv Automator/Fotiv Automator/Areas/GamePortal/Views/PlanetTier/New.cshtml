@model Fotiv_Automator.Areas.GamePortal.ViewModels.Forms.PlanetaryTierForm
@{
    ViewBag.Title = string.Format("New Planetary Tier");
}

<div class="row">
    @using (Html.BeginForm())
    {

        <div class="hidden" id="anti-forgery-container">
            @Html.AntiForgeryToken()
        </div>

        <div class="col-md-6">
            @Html.ValidationSummary()

            <div>
                @Html.LabelFor(x => x.Name)
                @Html.EditorFor(x => x.Name)
            </div>

            <div>
                @Html.LabelFor(x => x.BuildRate)
                @Html.EditorFor(x => x.BuildRate)
            </div>

            <input class="btn btn-default newPlanetaryTierButton" type="submit" value="Create Planetary Tier" />
        </div>
    }
</div>